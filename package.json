{
  "name": "takeoff",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5194 --strictPort",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4194 --strictPort"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "pdfjs-dist": "^3.11.174"
  }
}
